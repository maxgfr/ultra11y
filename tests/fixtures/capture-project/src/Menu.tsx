// Renders an intrinsic control but has NO capture → a blind spot (its runtime
// accessible name is unverifiable from source alone).
export function Menu() {
  return <button type="button">Open menu</button>;
}
