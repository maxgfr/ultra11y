// Dropped by the SECOND half of the comma-separated exclude value
// ("**/excluded/**,**/skipme/**") — pins the comma-splitting glob semantics.
export default function Nope() {
  return <button>Non</button>;
}
